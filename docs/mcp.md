# MCP

MCP servers are external providers of tools and resources. Discovered MCP tools
must enter the same canonical registry, policy evaluation, approval, execution
record, truncation, and observability paths as built-in tools.

MCP connection management and transport are staged. The runtime must not expose
every MCP schema to every Agent, and MCP server process/network permissions must
be enforceable independently from model instructions.
